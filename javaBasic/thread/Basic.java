package thread;

/**
 * @author honeyhyeok
 *
 */
public class Basic {
	
	/**
	 * 
	 * Thread 개요
	 * 다중 프로그래밍: 하나의 프로세서에서 여러개의 프로세스를 수행하는 것
	 * 다중 쓰레딩: 하나의 프로세스 내에 여러개의 쓰레드를 가지고 프로그램을 동시에 처리하는 것
	 * 
	 * !JSP기반의 웹어플리케이션의 경우 사용자를 쓰레드로 처리하기 때문에 다중 쓰레딩 필수
	 * 
	 * thread status
	 * 1. thread create
	 * 2. thread runnable
	 * 3. thread running
	 * 4. thread blocked
	 * 5. thread dead
	 * 각각의 상태는 Thread 클래스의 메소드로 제어
	 * 
	 * 
	 * 
	 * 
	 * 
	 */

}
